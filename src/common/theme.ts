import { createTheme } from '@mui/material/styles'

export const appTheme = createTheme({
  palette: {
    primary: {
      main: '#f2bf08',
    },
    secondary: {
      main: '#f5f8fa',
    },
    info: {
      main: '#f5f8fa',
    },
    success: {
      main: '#f5f8fa',
    },
  },
  typography: {
    fontFamily: 'Cardo, comic-sans, serif, -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Fira Sans, Droid Sans, Helvetica Neue, sans-serif',
  },
})
